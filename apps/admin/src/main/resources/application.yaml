spring:
  threads:
    virtual:
      enabled: true
  application:
    name: yogieat-admin
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.security.servlet.UserDetailsServiceAutoConfiguration
  config:
    import:
      - logging.yaml
      - datasource.yaml
      - client.yaml
      - ai.yaml

server:
  port: 8081

jwt:
  secret: ${JWT_SECRET:your-256-bit-secret-key-here-must-be-at-least-32-characters}
  access-token-validity: 3600000
  refresh-token-validity: 604800000

kakao:
  api:
    api-key: ${KAKAO_API_KEY:admin-dummy-key}
    base-url: ${KAKAO_API_BASE_URL:https://dapi.kakao.com}

ai:
  gemini:
    api-key: ${AI_GEMINI_API_KEY:${GEMINI_API_KEY:admin-dummy-key}}
    model: ${AI_GEMINI_MODEL:${GEMINI_MODEL:gemini-2.0-flash}}
