services:
  yogieat-api:
    image: ${IMAGE_FULL_URL}
    container_name: ${DOCKERHUB_IMAGE_NAME}
    restart: always
    ports:
      - "8080:8080"
    environment:
      - TZ=Asia/Seoul
      - DATASOURCE_DB_CORE_JDBC_URL=jdbc:postgresql://yogieat-db:5432/yogieat
    networks:
      - yogieat-network
    env_file:
      - .env
    depends_on:
      - yogieat-db
  yogieat-db:
    image: postgis/postgis:15-3.3
    container_name: yogieat-db
    restart: always
    ports:
      - "5432:5432"
    environment:
      DATASOURCE_DB_CORE_JDBC_URL: yogieat-db
      DATASOURCE_DB_CORE_USERNAME: ${DATASOURCE_DB_CORE_USERNAME}
      DATASOURCE_DB_CORE_PASSWORD: ${DATASOURCE_DB_CORE_PASSWORD}
      TZ: Asia/Seoul
    command: [ "postgres", "-c", "log_statement=all" ]
    volumes:
      - ./docker/init.sql:/docker-entrypoint-initdb.d/init.sql
#  TODO: Redis, etc.
    networks:
      - yogieat-network

networks:
  yogieat-network:
    driver: bridge

volumes:
  postgres_data:
  mongodb_data:
