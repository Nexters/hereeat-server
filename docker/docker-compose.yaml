version: "3.8"

services:
  yogieat-api:
    image: ${IMAGE_FULL_URL}
    container_name: ${DOCKERHUB_IMAGE_NAME}
    restart: always
    environment:
      - TZ=Asia/Seoul
    network_mode: host
    env_file:
      - ~/.env
#  TODO: Redis, etc.
