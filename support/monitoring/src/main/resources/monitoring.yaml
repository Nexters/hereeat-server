management:
  endpoints:
    web:
      exposure:
        include: health
      base-path: /actuator
    jmx:
      exposure:
        exclude: "*"
  health:
    defaults:
      enabled: true
  tracing:
    sampling:
      probability: 1.0  # 100% sampling for development (default is 0.1)
